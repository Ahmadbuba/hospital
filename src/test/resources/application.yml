spring:
  datasource:
    url: jdbc:tc:postgresql:14.4:///integration-tests-db
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false